# FewToken Aqua 接入接口

[English API reference](INTEGRATION.md)

已把现有策略整理成 SDK：合作方可以取得建仓、关闭、包装和解包的交易数据，以及完整兑换的执行步骤。本轮不做前端页面，不部署新生产合约，也不自动签名或发交易。

采用与 Barker 公开案例一致的官方 Aqua/SwapVM 合约方式。接口由 Ring 定义；参考同一种合作模式，不代表 1inch 已经接受我们的接口或完成 FewToken 路由接入。

Powered by SwapVM — © Degensoft Ltd 2025. Powered by Aqua — © Degensoft Ltd 2025. [许可范围](LICENSE.md)。

## 可以直接使用的接口

| 接口 | 做什么 | 注意事项 |
| --- | --- | --- |
| `buildAquaShipPlan` | 生成限额授权和建仓交易，返回策略身份 | 建仓前应有足够 FewToken；不会自动动用资金 |
| `buildAquaDockPlan` | 生成关闭和撤销授权交易 | 使用最初配置重建身份，策略过期后仍可关闭 |
| `buildMakerWrapPlan` | 生成 USDC/USDT → FewToken 的包装调用 | 只支持固定的两种资产，限额授权，包装后清理临时授权 |
| `buildMakerUnwrapPlan` | 生成 FewToken → USDC/USDT 的解包调用 | 资产回到同一个 maker；需要实时余额和即时赎回资金支持 |
| `buildUnderlyingRoute` | 生成普通币完整兑换步骤、报价调用和内部 swap 数据 | 必须由执行方在一笔交易内完成，包含实际输出解包和剩余输入退款 |

默认入口为 `index.mjs`；不依赖 Node 文件读取的计算入口为 `portable.mjs`，由使用方传入锁定版本的官方 SDK。两个入口输出一致性已测试；具体浏览器构建和页面接入不在本轮验收内。

## 包装和解包的输入

```json
{
  "chainId": 1,
  "maker": "实际使用的做市钱包地址",
  "asset": "USDC",
  "amount": "1250001"
}
```

`asset` 仅支持 USDC、USDT。这里的 amount 是原始整数单位，`1250001` 代表 1.250001 USDC；策略配置中的 `fwUSDC`、`fwUSDT` 仍采用人类可读单位，不能混用。

包装过程为：清零旧授权 → 授权明确额度 → 包装回本钱包 → 清理授权。解包直接调用 FewToken 的 `unwrapTo`，不需要再授权别的合约来销毁自己的 FewToken。

做市资金准备允许分笔操作。如果授权成功而包装失败，仍可能留下授权，调用方需要显示失败并提供撤销入口。关闭后的解包数量应读取当时余额决定，不能照抄最初的存入数量。

## 完整普通币兑换

支持 USDC → fwUSDC → Aqua → fwUSDT → USDT，以及反向路径；支持指定输入金额和指定输出金额。

SDK 返回的是供执行方使用的步骤描述，不是可以直接广播的一笔交易。它明确指定代币、官方 Router、执行账户、最终收款地址、输入上限、输出下限和期限。执行方必须在同一笔交易里完成：

1. 从执行方已授权使用的库存取出有限输入金额并包装。
2. 调用官方 SwapVM，检查实际输入输出及限价。
3. 按实际成交返回量解包输出，交给约定的结算地址。
4. 指定输出金额时，把没有用完的输入解包退回执行方。
5. 清理临时授权，并确认没有误转原有余额或留下本次交易资产。

用户订单的签名、资金来源授权及最终交付检查仍属于执行方外层结算。不能把旧报价填成固定解包数量，也不能要求用户分三笔交易操作来冒充原子兑换。

`test/RecipeHarness.sol` 是解释这些步骤的本地测试程序，已经验证实际输出、退款、原有余额保留和失败回滚。它仍缺少正式执行方的完整订单授权、部署和路由控制，不能直接部署用于接单。具体执行方不支持这些步骤时，需要为其运行环境补适配器；返回值中的 `adapterStatus` 明确保留这一状态。

## 测试与联调边界

2026-09-11：**45 项离线测试及 38 项主网 fork 用例全部通过**，包含原有回归。新增覆盖两种普通币的包装与解包、完整路线的双向及两种金额模式、剩余输入退款、原有资产保留、滑点或解包失败时整笔回滚，以及过期仓位关闭。[原始报告](evidence/integration-2026-09-11/fork-results.json)。

所有资金、部署、授权和成交都发生在本地 Anvil。没有动用主网资金，也没有与真实 1inch 执行方完成联调。

接下来交给合作方的材料是本 SDK、英文接口说明和可复现测试。需要对方落实的事项是：

- 接入报价与执行系统的具体接口、代码提交位置和评审联系人。
- 哪个具备资格的执行方参与，是否支持按成交返回量解包和退回剩余输入。
- 用 1inch 哪个交易入口做试点，如何验收价格有优势时普通 USDC/USDT 订单实际经过 FewToken 成交。

Ring 可以承担对方开放范围内的适配开发；这些通用接口和本地测试不需要等对方设计后才开始。前端展示、奖励和推广另行讨论。
